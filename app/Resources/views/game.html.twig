{# app/Resources/views/game.html.twig #}
<!DOCTYPE html>
<html>
    <head>
        <title>Welcome</title>
        <link rel="stylesheet" href="{{ asset('css/main.css') }}" />
    </head>
    <body>
    <div class='text'><h3>{{message}}</h3></div>
        <ul id="navigation">
           
            {{ form_start(form) }}
             <img src="{{ asset('img/Wheel_of_Fortune.png') }}"/> 
            {{ form_widget(form) }}
            {{ form_end(form) }}
            <a href="inloggen">Afmelden</a>  
        </ul>
    </body>
</html>